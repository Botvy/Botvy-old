/**
 * Defines the structure for all theme files
 *
 * @export
 * @interface ITheme
 */
export interface ITheme {
    color: {
        background: string;
        foreground: string;
        error: string;
    };
}
